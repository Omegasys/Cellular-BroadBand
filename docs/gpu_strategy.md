GPU Strategy
Goal

Build a fully open graphics pipeline compatible with Vulkan.

Approach
Tile-based rendering architecture
Mesa-compatible driver stack
Avoid vendor-specific shader IRs
Reality Constraints
Performance gap vs Adreno/Apple GPUs
Driver maturity is the biggest bottleneck
Ecosystem
Vulkan (primary API)
Open-source drivers inspired by Panfrost
